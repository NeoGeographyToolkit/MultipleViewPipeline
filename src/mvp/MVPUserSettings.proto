package mvp;

message MVPUserSettings {
  required double alt_min = 1;
  required double alt_max = 2;

  optional bool use_octave = 3 [default=false];
  optional bool test_algorithm = 4 [default=true];
}
